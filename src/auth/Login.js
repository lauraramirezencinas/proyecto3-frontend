import React, { Component } from 'react'
import { Button, FormGroup, FormControl, FormLabel, FormText, Form } from 'react-bootstrap'

export class Login extends Component {
    render() {
        return (
            
        )
    }
}

export default Login
